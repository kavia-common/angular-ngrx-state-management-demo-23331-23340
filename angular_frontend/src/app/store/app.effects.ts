import { Injectable } from '@angular/core';
import { Actions } from '@ngrx/effects';

/**
 * Root-level effects placeholder for future side effects.
 */
@Injectable()
export class AppEffects {
  constructor(private actions$: Actions) {}
}
